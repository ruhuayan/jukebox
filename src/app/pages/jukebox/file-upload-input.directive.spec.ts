import { AppFileUploadInput } from './file-upload-input.directive';

describe('FileUploadInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFileUploadInput();
    expect(directive).toBeTruthy();
  });
});
